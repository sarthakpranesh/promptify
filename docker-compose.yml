services:
  promptify:
    build:
      context: .
      dockerfile: Dockerfile
    image: sarthakpranesh/promptify:0.0.1
    container_name: promptify
    restart: unless-stopped
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - ./container_data:/app/data
