services:
  promptify:
    image: sarthakpranesh/promptify:latest
    container_name: promptify
    restart: unless-stopped
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - ./promptify_data:/app/data
